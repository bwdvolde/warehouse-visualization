

export default class Circle {
    x: number;
    y: number;
    selected: boolean;


    constructor(x: number, y: number, selected: boolean) {
        this.x = x;
        this.y = y;
        this.selected = selected;
    }
}
